public class strass24 {
    public static void main(String[] args) 
    {
        String str="";
        int countpuncmarks=0;
        for (int i=0;i<str.length;i++)
        {
            if(str.charAt(i)==||str.charAt(i)==||str.charAt(i)==||str.charAt(i)==||str.charAt(i)==||str.charAt(i)==||str.charAt(i)==||)
            {
                countpuncmarks++;
            }
        }
        System.out.println("total number of punctuatios exists in string is : " + countpuncmarks);
    }
    
}
